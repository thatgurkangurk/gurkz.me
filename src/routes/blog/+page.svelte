<h2 class="text-3xl">the blog is currently undergoing maintenance. please check back later</h2>

<!-- <script lang="ts">
	import type { PageData } from "./$types";
	import { Button } from "$lib/components/ui/button";
	import * as Card from "$lib/components/ui/card";

	export let data: PageData;
</script>

<h2 class="text-3xl">posts</h2>
<div class="grid grid-cols-1 gap-4 md:grid-cols-3">
	{#each data.posts as post}
		<Card.Root>
			<Card.Header>
				<Card.Title>{post.title}</Card.Title>
				<Card.Description>by {post.expand.author.name}</Card.Description>
			</Card.Header>
			<Card.Footer>
				<Button href={`/blog/${post.slug}`}>read</Button>
			</Card.Footer>
		</Card.Root>
	{/each}
</div> -->
